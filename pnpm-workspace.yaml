# pnpm v10+ blocks package build scripts by default. These two are required:
#   esbuild        - Vite's transform pipeline
#   unrs-resolver  - eslint-import-resolver-typescript's native resolver
allowBuilds:
  esbuild: true
  unrs-resolver: true
