/** localStorage key holding the bearer token. Read by http.ts and the auth provider. */
export const TOKEN_KEY = 'react-starter.token';

/** Default page size for every server-side table. */
export const DEFAULT_PAGE_SIZE = 10;
