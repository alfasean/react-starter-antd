# true  = run entirely on the in-memory mock data provider (no backend needed)
# false = talk to VITE_APP_API_URL
VITE_API_MOCK=true

VITE_APP_NAME=React Starter

# Required when VITE_API_MOCK is not "true".
VITE_APP_API_URL=https://api.example.com/v1
